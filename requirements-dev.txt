# Test/dev dependencies (not needed in the deployed Space).
# Install alongside the runtime deps:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest>=7.4
pytest-timeout>=2.2
